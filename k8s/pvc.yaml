apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: commerce-data
  namespace: hanzo
  labels:
    app: commerce
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: do-block-storage
