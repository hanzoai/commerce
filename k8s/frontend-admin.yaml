apiVersion: apps/v1
kind: Deployment
metadata:
  name: commerce-admin
  namespace: hanzo
  labels:
    app: commerce-admin
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: commerce-admin
  template:
    metadata:
      labels:
        app: commerce-admin
    spec:
      containers:
        - name: admin
          image: ghcr.io/hanzoai/commerce-admin:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: commerce-admin
  namespace: hanzo
spec:
  selector:
    app: commerce-admin
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: commerce-admin
  namespace: hanzo
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - admin.commerce.hanzo.ai
      secretName: commerce-admin-tls
  rules:
    - host: admin.commerce.hanzo.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: commerce-admin
                port:
                  number: 80
