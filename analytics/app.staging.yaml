application: crowdstart-staging
module: analytics
version: v1

runtime: go
api_version: go1
threadsafe: true

automatic_scaling:
  max_concurrent_requests: 100
  min_idle_instances: 1

handlers:
- url: /.*
  script: _go_app
  secure: always
