# Allow esbuild's postinstall to run in non-interactive builds. The
# only postinstall in this dep tree is esbuild's platform-specific
# binary install; we already trust it via package.json's
# `pnpm.onlyBuiltDependencies`.
auto-install-peers=true
strict-peer-dependencies=false
