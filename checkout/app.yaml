application: crowdstart-io
module: checkout
version: v1

runtime: go
api_version: go1
threadsafe: true

instance_class: F2
automatic_scaling:
  min_idle_instances: 1

handlers:
- url: /checkout.*
  script: _go_app
