import { ComponentExample } from "@/components/ComponentExample"
import { ComponentReference } from "@/components/ComponentReference"

export const metadata = {
  title: `Command`,
}

# {metadata.title}

A component that renders an unhighlighted code block, useful for one-liners or API routes.

In this guide, you'll learn how to use the Command component.

<ComponentExample name="command-demo" />

## Usage

```tsx
import { Command } from "@hanzo/commerce-ui"
```

```tsx
<Command>
  <code>yarn add @hanzo/commerce-ui</code>
</Command>
```

---

## API Reference

<ComponentReference mainComponent="Command" />

---

## Usage Outside Hanzo Commerce Admin

If you're using the `Command` component in a project other than the Hanzo Commerce Admin, make sure to include the `TooltipProvider` somewhere up in your component tree, as the `Command.Copy` component uses a [Tooltip](../tooltip/page.mdx#usage-outside-hanzo-commerce-admin):

```tsx
<TooltipProvider>
  <Command>
    <code>yarn add @hanzo/commerce-ui</code>
    <Command.Copy
      content="yarn add @hanzo/commerce-ui"
      className="ml-auto"
    />
  </Command>
</TooltipProvider>
```
