Embed target for the billing admin SPA (Next.js static export).
The billing repo builds it and publishes it as ghcr.io/hanzoai/billing; the
Dockerfile copies /public out of that image here before the Go build. Only
this file is tracked so go:embed resolves on a fresh clone; every other
artifact comes from that image.
